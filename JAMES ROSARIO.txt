from math import ceil


passenger_name = str(input("input your name"))
flight_number = str(input("number"))
bag_weight = float(input("bag weight:  "))
is_carry_on = bool(input("carry on or no True or False"))
is_interanational = bool(input("international or no: "))
is_elite = bool(input("elite or no:   "))
billable_kg = ceil(bag_weight)
base_fee = (billable_kg * 3.00) + 15.00

if bag_weight < 8:
    print("Free carry total = 0.00")
    if bag_weight >= 8:
        print("totl = 35")
    elif bag_weight >= 32:
        print("total = 120")
        if is_elite == True and bag_weight <= 23:
            print("toatal = 0")
            if is_elite == True and bag_weight >= 23:
                  print(f"toatal = {base_fee * 0.50}")
                  if bag_weight <= 32 and is_elite == " " and is_interanational == True:
                      print(f"toatal is {base_fee * 20.00}")
    else:
        print(f"standard {base_fee} ")
